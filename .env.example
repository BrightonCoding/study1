# Copy to .env and fill in. .env is gitignored — NEVER commit real keys.
# Only needed when collection.backend: youtube_api  (the yt-dlp default needs no key).
YOUTUBE_API_KEY=your_youtube_data_api_v3_key_here
