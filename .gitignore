# secrets
.env
.env.local
.env.*.local

# python
__pycache__/
*.pyc
.venv/
.ipynb_checkpoints/

# data artifacts — large / regenerable. Seed CSV + data dictionary ARE versioned (see !below).
data/raw/
data/thumbnails/
data/embeddings/
data/panels/
outputs/logs/

# results ARE versioned (small + the point of the repo): FINDINGS.md, tables, plots.
# Only logs and large/regenerable data are excluded above.

# keep the reproducibility anchors and docs
!data/seeds/
!data/seeds/seed_channels.csv
!docs/

# macOS
.DS_Store

# harness
.claude/
