# secrets
.env
.env.local
.env.*.local

# python
__pycache__/
*.pyc
.venv/
.ipynb_checkpoints/

# data artifacts — large / regenerable, at ANY niche depth (data/raw, data/gaming/raw, ...).
# ** matches zero or more dirs, so these cover both finance (top-level) and per-niche folders.
data/**/raw/
data/**/thumbnails/
data/**/embeddings/
data/**/panels/

# logs at any depth may contain API-key-bearing error text — never commit them.
outputs/**/logs/
outputs/logs/

# results ARE versioned (small + the point of the repo): FINDINGS.md, tables, plots, summary.
# Only logs and large/regenerable data are excluded above. Seed CSVs (data/**/seeds/) are kept.

# macOS
.DS_Store

# harness
.claude/
